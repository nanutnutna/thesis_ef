# Core FastAPI dependencies (จำเป็น)
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20

# Elasticsearch client (จำเป็น)
elasticsearch==8.17.0

# Sentence Transformers and ML dependencies (จำเป็น)
sentence-transformers==3.4.0
torch==2.5.1
transformers==4.48.1
numpy==1.26.4
scikit-learn==1.4.1.post1

# Supporting dependencies (จำเป็น)
requests==2.31.0
tokenizers==0.21.0
huggingface-hub==0.27.1
safetensors==0.5.2

# Utilities (จำเป็น)
python-dotenv==0.21.0
pydantic==2.10.5
typing-extensions==4.12.2

# Performance dependencies
tqdm==4.67.1